let numbers = [97, 1, 50, 90, 100, -5, -20, 9, 0];

let max = numbers[0];
let min = numbers[0];
for (number of numbers) {
  if (number > max) {
    max = number;
  }
  if (number < min) {
    min = number
  }
}

console.log(max, min); // 100, -20